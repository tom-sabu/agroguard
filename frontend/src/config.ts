// Configuration file for API URL

// When running on mobile, replace 'localhost' with your computer's local IP address.
// Example: "http://192.168.1.5:8000"
// You can find your IP by running `ipconfig` (Windows) or `ifconfig` (Mac/Linux) in the terminal.

export const API_BASE_URL = "http://172.17.30.192:8000"
